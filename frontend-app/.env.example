# Backend API base URL (no trailing slash)
REACT_APP_API_URL=http://localhost:3000

# Dev/test convenience — shared password for the demo accounts auto-created by
# the backend devBootstrap.  Only shown in the login UI when NODE_ENV !== "production".
# Falls back to "DevPass123!" if unset.
REACT_APP_TEST_USERS_PASSWORD=DevPass123!
